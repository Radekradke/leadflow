# URL PÚBLICA do backend. Em produção o front chama o backend DIRETO
# (sem o proxy /api do dev). Aponte para o domínio real da API.
#   Domínios separados:  https://leadflow-api.onrender.com
#   Subdomínio próprio:  https://api.seudominio.com
VITE_API_URL="https://SUA-API.onrender.com"
